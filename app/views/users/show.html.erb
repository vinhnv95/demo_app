<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<h2>Micropost</h2>
<table>
	<thead>
		<tr>
			<th>ID</th>
			<th>Content</th>
		</tr>
	</thead>
	<tbody>	
		<% @user.microposts.each do |x|%>
		<tr>
			<td><%= x.id%></td>
			<td><%= x.content%></td>
		</tr>
		<% end %>
	</tbody>
</table>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
